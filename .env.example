#
# .env.example - File mẫu cho biến môi trường cục bộ
# Tạo một file .env thực tế từ file này và điền thông tin của bạn.
# File .env sẽ KHÔNG được commit vào Git để bảo mật.
#

# --- Database (PostgreSQL) ---
POSTGRES_DB=booklendingdb
POSTGRES_USER=bookuser
POSTGRES_PASSWORD=bookpassword123